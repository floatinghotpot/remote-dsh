{
  "name": "rdsh-hub",
  "version": "0.2.1",
  "description": "rdsh server: control plane (auth, host registry, routing) + data plane (tunnel relay)",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "rdsh-tunnel": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/"
  }
}
