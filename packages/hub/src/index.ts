/**
 * rdsh-hub — server. Prototype phase: TypeScript (ships inside the `rdsh` npm
 * package as `rdsh hub ...`). Production phase: Go single binary (see
 * doc/marketing/proposal.md §7).
 */
export const NAME = "rdsh-hub";
