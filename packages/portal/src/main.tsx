import React from "react";
import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <div>rdsh portal — lands with M2 (hub)</div>
  </React.StrictMode>,
);
