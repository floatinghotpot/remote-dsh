{
  "name": "rdsh-portal",
  "version": "0.2.0",
  "description": "rdsh web portal: login + host list (Vite + React 18, mirrors DSH frontend stack)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "node scripts/build-legal.mjs && tsc --noEmit -p tsconfig.json",
    "build": "node scripts/build-legal.mjs && tsc --noEmit -p tsconfig.json && vite build",
    "dev": "vite",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.8"
  }
}
