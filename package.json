{
  "name": "remote-dsh",
  "version": "0.1.0",
  "private": true,
  "description": "Secure remote access layer for DeepSeek Harness (rdsh)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
